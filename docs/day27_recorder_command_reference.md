# Day27 recorder command reference

Recorder command is unchanged from Day24-26. The row-specific v2 ambiguity variant
is an admin-only operator instruction and is not passed to the recorder/model.

Use `python scripts/generate_day27_recorder_commands.py` to print the 15 assigned
variants and the existing Windows recorder command.
